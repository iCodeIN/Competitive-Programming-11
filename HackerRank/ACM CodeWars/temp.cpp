#include <bits/stdc++.h>
using namespace std;

struct node
{

    int x;

    bool operator<(node v)
    {
        return x < v.x;
    }
};

int main()
{
    node A[100];

    sort(A, A + 100);

    cout << " ksfp[b-jhe";
}